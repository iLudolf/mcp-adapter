/**
 * Injection token for MCP module options.
 */
export const MCP_MODULE_OPTIONS = "MCP_MODULE_OPTIONS"

/**
 * Injection token for the MCP logger.
 */
export const MCP_LOGGER = "MCP_LOGGER"

/**
 * Injection token for the McpServer instance.
 */
export const MCP_SERVER_INSTANCE = "MCP_SERVER_INSTANCE"

/**
 * Default base path for MCP HTTP endpoints.
 */
export const DEFAULT_MCP_PATH = "mcp"

/**
 * Default logger context.
 */
export const MCP_LOGGER_CONTEXT = "Mcp-server"
